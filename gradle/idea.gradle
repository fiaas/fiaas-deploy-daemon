apply plugin: 'idea'

idea {
    project {
        vcs = 'Git'
    }
    module {
        sourceDirs += file('fiaas_deploy_daemon')
        sourceDirs += file('k8s')
        testSourceDirs += file('tests')
    }
}
