apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiaas-deploy-daemon.fullname" . }}-test-connection"
  labels:
{{ include "fiaas-deploy-daemon.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fiaas-deploy-daemon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
